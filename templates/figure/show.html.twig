{% extends 'base.html.twig' %}

{% block title %}Figure{% endblock %}

{% block body %}
    <h1>Figure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ figure.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ figure.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('figure_index') }}">back to list</a>

    <a href="{{ path('figure_edit', {'id': category.id}) }}">edit</a>

    {{ include('figure/_delete_form.html.twig') }}
{% endblock %}
